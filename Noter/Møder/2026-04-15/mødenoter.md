figure out what our closing argument about slm in quest generation is gonna be

rag is when you are assisting the genration by picking out relevant information and feeding it to the ai model, the rag model is running besides the ai model, this is mortens definition

try looking at perplexity for sources on llms as a judge of video game quests

look at tokens for figuring out why the models sometimes break down

try looking into using llms as judge of stories, instead of video game quests, and then reference them when we talk about judging

remember to not get lost in the sauce, looking for perfection and instead cut your losses and write a good report instead

remember not to call things we are missing as shortcomings, but instead as future improvements

try swapping which order of outputs works best, maybe it works better generating the description before the quest structure. although it probably wont

try running experiments like, what happens if when you get bad output, and you try repeating this, see if the same input gives better output and recovers, or if the input consistently gives bad outputs.

try generating many data points and draw conclusions from that, don't run something once or twice and call it a day

the models can compound errors if it makes a bad description early on

censor would like to see all our things, so find a way to show our ai model